name: Block Direct Main Push

on:
  push:
    branches:
      - main

jobs:
  block:
    runs-on: ubuntu-latest
    steps:
      - name: Fail on direct push to main
        run: |
          echo "Direct pushes to main are not allowed. Please use a PR."
          exit 1
